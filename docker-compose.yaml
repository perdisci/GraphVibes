services:
  graphvibes:
    build: .
    ports:
      - "3300:3000"
    volumes:
      - .:/app
      - /app/node_modules
    environment:
      - NODE_ENV=development
    networks:
      - jgnet

networks:
  jgnet:
    external: true
